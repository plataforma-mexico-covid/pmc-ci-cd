apiVersion: v1
kind: Service
metadata:
  name: {{ include "platformaui.fullname" . }}
  labels:
    {{- include "platformaui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "platformaui.selectorLabels" . | nindent 4 }}
