apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pmc-analizer.fullname" . }}-test-connection"
  labels:
    {{- include "pmc-analizer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pmc-analizer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
