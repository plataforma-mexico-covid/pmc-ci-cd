apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "platformaui.fullname" . }}-test-connection"
  labels:
    {{- include "platformaui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "platformaui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
